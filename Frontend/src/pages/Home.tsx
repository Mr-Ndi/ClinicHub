import Hero from '../components/Hero';

const Home = () => {
    return (
        <div className="flex-grow">
            <Hero />
            {/* Add more home page content here if needed */}
        </div>
    );
};

export default Home;
